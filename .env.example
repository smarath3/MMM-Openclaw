# MMM-Openclaw Development Environment
# =====================================
# Copy this to .env and fill in your values:
#   cp .env.example .env
#
# .env is git-ignored. Never commit secrets.

# OpenClaw Gateway (your OpenClaw instance)
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789
OPENCLAW_GATEWAY_TOKEN=

# Raspberry Pi deployment target
PI_HOST=pi@raspberrypi.local
PI_MODULE_PATH=/home/pi/MagicMirror/modules/MMM-Openclaw

# OpenClaw skills path on Pi (if OpenClaw runs on the Pi)
# OPENCLAW_SKILLS_PATH=/home/pi/.openclaw/skills

# Optional: Tailscale hostname for remote Pi
# PI_HOST=pi@your-pi.tail12345.ts.net

# AWS OpenClaw host (for skill deployment)
# AWS_OPENCLAW_HOST=ubuntu@ec2-your-instance.compute.amazonaws.com
# AWS_OPENCLAW_KEY=path/to/your-key.pem
# OPENCLAW_SKILLS_PATH=/home/ubuntu/.openclaw/skills
